{
  "name": "gtcm-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "admin",
    "delivery",
    "user"
  ],
  "scripts": {
    "build": "cd user && npm install && npm run build",
    "build:user": "cd user && npm install && npm run build",
    "build:admin": "cd admin && npm install && npm run build",
    "build:delivery": "cd delivery && npm install && npm run build"
  }
}
